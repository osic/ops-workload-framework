heat_template_version: 2015-04-30
description: Control Plane workload.

parameters:
  availability_zone:
    type: string
    description: The Availability Zone to launch the instance.
  key:
    type: string
    description: Key pair used to ssh into instance.
  volume_size:
    type: number
    description: Size of the volume to be created.
    default: 1
    constraints:
      - range: { min: 1 }
  image_id:
    type: string
    label: Image ID
    description: Image to be used for compute instance
    constraints:
      - custom_constraint: glance.image
  instance_type:
    type: string
    label: Instance Type
    description: Type of instance (flavor) to be used
    constraints:
      - custom_constraint: nova.flavor
  network_id:
    type: string
    label: Network ID/Name
    description: Network to be used for compute instance.
resources:
  name_nonce:
    type: OS::Heat::RandomString
    properties:
            length: 7
            sequence: lowercase
  nova_server:
    type: OS::Nova::Server
    properties:
      name:
        str_replace:
          template: control-$NONCE
          params:
            $NONCE: { get_resource: name_nonce}
      availability_zone: { get_param: availability_zone }
      key_name: { get_param: key }
      networks:
        - network: { get_param: network_id }
      image: { get_param: image_id }
      flavor: { get_param: instance_type }
      user_data_format: RAW
      user_data:
        str_replace:
          params:
            __image_id__: { get_param: image_id }
            __flavor__: { get_param: instance_type }
            __availability-zone__: { get_param: availability_zone }
            __network__: { get_param: network_id }
            __key-name__: { get_param: key }
          template: |
            #!/bin/bash
            sudo apt-get update
            sudo apt-get install python-openstackclient
            export OS_AUTH_URL=https://cloud1.osic.org:5000/v3
            export OS_TENANT_ID=ddd81f9ab03f45e09196169abceaec6f
            export OS_TENANT_NAME="osic-engineering"
            export OS_USERNAME="shashank.tavildar"
            export OS_PASSWORD="Sh@nks0791"
            export OS_REGION_NAME="RegionOne"
            export OS_ENDPOINT_TYPE=publicURL
            export OS_INTERFACE=public
            export OS_IDENTITY_API_VERSION=2
            i=0
            while true; do
                openstack server create --image '__image_id__' --flavor '__flavor__' --key-name '__key-name__' --nic net-id='__network__' --availability-zone '__availability-zone__' controlPlane.$i
                openstack server list
                openstack server delete controlPlance.$i
                i++
                sleep 20
                openstack server create --image '__image_id__' --flavor '__flavor__' --key-name '__key-name__' --nic net-id='__network__' --availability-zone '__availability-zone__' controlPlane.$i
                openstack server list
                openstack server delete controlPlance.$i
                i++ 
            done
